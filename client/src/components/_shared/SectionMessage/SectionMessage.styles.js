import { css } from 'emotion';
import { greyLight } from '../../../lib/colors.styles';

const container = css`
  color: ${greyLight};
  font-size: 24px;
  margin-top: 20px;
  text-align: center;
`;

export default container;
